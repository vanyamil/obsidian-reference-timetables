import {TableGenSettings} from './types';

export const DEFAULT_GEN_SETTINGS: TableGenSettings = {
	time_axis_path: "",
	inquire_axis_path: "",
	collect_links: "front",
	include_first: true,
	include_last: true,
};

export const PLUGIN_TITLE: string = "Reference Timetables";